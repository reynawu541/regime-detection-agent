# Optional. Without this, the analyst/report-writer step falls back to a
# deterministic template instead of calling Claude -- the pipeline still
# produces a complete daily note with no API key required.
ANTHROPIC_API_KEY=

# Optional override; defaults to config/config.yaml's llm.default_model.
ANTHROPIC_MODEL=
